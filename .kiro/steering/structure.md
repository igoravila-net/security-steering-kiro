# Project Structure

```
.kiro/
  steering/       # AI steering documents (conventions, rules, context)
```

The project is currently empty. Update this file as source directories and modules are added.
